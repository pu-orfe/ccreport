# Python environment and cache files
.venv/
venv/
env/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Packaging build artifacts
build/
dist/
*.egg-info/
.eggs/

# Local configuration and credentials
.env
.env.deploy
*.pem
service-account*.json

# Local state. The deploy ledger records which steps a *particular* environment
# has completed, and `deploy` resumes from it — so committing one would make a
# fresh clone skip provisioning steps it has never run.
.ccreport/

# Locally generated report bundles and rendered artifacts
bundles/
artifacts/
scratch/

# System files
.DS_Store

# Agent files
.claude/
CLAUDE.md
.ralph/
